# .env 파일에 재정의할 환경 변수를 이 파일에 명시합니다.

# 애플리케이션 환경 (development/staging/production)
# APP_ENV=development

# 디버그 모드
# DEBUG=True

# 앱 시작 시 테이블 자동 생성 (개발 환경에서만 사용, 운영 환경에서는 False로 설정)
# AUTO_CREATE_TABLES=True

# 데이터베이스 URL
# DATABASE_URL=sqlite+aiosqlite:///./commerce.db

# JWT 설정
# JWT_SECRET_KEY=your-secret-key-here
# JWT_ALGORITHM=HS256
# JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
